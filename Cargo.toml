[package]
name = "midnight"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.17.3", features = ["bevy_dev_tools", "dynamic_linking"] }
midly = "0.5.3"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3